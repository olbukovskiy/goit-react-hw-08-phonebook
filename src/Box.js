import styled from "@emotion/styled";
import {
  space,
  layout,
  color,
  typography,
  flexbox,
  shadow,
  background,
  border,
  position,
  grid,
} from "styled-system";

const Box = styled.div`
  ${space}
  ${layout}
  ${color}
  ${typography}
  ${flexbox}
  ${background}
  ${border}
  ${position}
  ${shadow}
  ${grid}
`;

export default Box;
